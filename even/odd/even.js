// 1. Even or Odd:
//  Write a program that takes an integer input and prints whether it's even or odd.

function isEven(int) {
  let isEven = true;
  if (int % 2 === 0) {
    return isEven;
  } else {
    isEven = false;
    return isEven;
  }
}
